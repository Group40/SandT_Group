import 'package:flutter/material.dart';

class ArticleHandling extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Punsara'),
    );
    throw UnimplementedError();
  }
}
