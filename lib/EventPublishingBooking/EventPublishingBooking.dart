import 'package:flutter/material.dart';

class EventPublishingBooking extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Piyumal'),
    );
    throw UnimplementedError();
  }
}