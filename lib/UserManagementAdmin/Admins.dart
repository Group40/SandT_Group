import 'package:flutter/material.dart';

class Admins extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('existing admins'),
    );
    throw UnimplementedError();
  }
}